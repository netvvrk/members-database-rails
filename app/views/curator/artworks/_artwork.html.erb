<tr>
  <td><%= artwork.id %></td>
  <td><%= artwork.user.name %></td>
  <td><%= link_to artwork.title, curator_artwork_url(artwork) %></td>
  <td><%= artwork.medium %></td>
  <td width="300px"><%= artwork.description %></td>
  <td>
    <% artwork.images.each do |image| %>
      <%= image_tag thumbnail_url(image)  %>
    <% end %>
  </td>
</tr>
